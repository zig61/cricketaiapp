PORT=8000
ENV=development

# Path to the MediaPipe Pose Landmarker model file. Not committed to git —
# run scripts/download_model.sh locally, or rely on the Dockerfile's build-time
# fetch for deployment. Default matches both.
POSE_MODEL_PATH=models/pose_landmarker_lite.task
