fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.5.0

# Pinned together — verified working combination. mediapipe 1.0.1 (latest)
# requires macOS 14+ at the native-library level and dropped the legacy
# mp.solutions.pose API entirely; 0.10.21 is the last release confirmed to
# work with the Tasks API (PoseLandmarker) in this environment. opencv's
# newest release wants numpy>=2, which conflicts with what mediapipe 0.10.21
# needs — numpy<2 keeps both happy together.
mediapipe==0.10.21
opencv-python-headless<5
numpy<2
httpx>=0.27.0
